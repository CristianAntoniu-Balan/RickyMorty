const parsedCharacter = (fetchedCharData) => {
};

const parsedLocation = (fetchedLocationData) => {
};

const parsedEpisode = (fetchedEpisodeData) => {
   const episodeFromAPI = ;
};

export const parsedItem = (context, item) => {};
